<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="modelsViewStyle">@style/ModelsViewStyle</item>
    </style>
    
    <style name="NavDrawerButton" parent="android:Widget.Holo.Button.Borderless">
        <item name="android:textColor">#cccccc</item>
        <item name="android:padding">10dp</item>
        <item name="android:gravity">left|center_vertical</item>
        <item name="android:drawablePadding">5dp</item>
    </style>

    <style name="ModelsViewStyle">
        <item name="android:background">@drawable/rounded_rect_bg</item>
        <item name="android:height">100dp</item>
    </style>

</resources>
